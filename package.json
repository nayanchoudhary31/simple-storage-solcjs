{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "5.7.2",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --abi --bin --include-path node_modules --base-path . -o . SimpleStorage.sol"
  }
}
